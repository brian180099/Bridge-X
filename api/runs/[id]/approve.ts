import { body, decideRun, json, options, runs } from "../../_orchestration.js";
export default { async fetch(request: Request, context: { params: { id: string } }) { if (request.method === "OPTIONS") return options(); if (request.method !== "POST") return json({ error: "METHOD_NOT_ALLOWED" }, 405); const run = runs.get(context.params.id); if (!run) return json({ error: "RUN_NOT_FOUND" }, 404); const input = await body(request); if (!["keep-8","change-10"].includes(String(input.decision)) || !String(input.reason ?? "").trim()) return json({ error: "INVALID_DECISION" }, 400); const next = decideRun(run, input.decision as any, String(input.reason), String(input.approver ?? "최종 승인자")); runs.set(next.id, next); return json(next); } };
