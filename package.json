{
  "name": "bridge-x",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c yellow,cyan \"npm run dev --workspace @bridge-x/api\" \"npm run dev --workspace @bridge-x/web\"",
    "dev:api": "npm run dev --workspace @bridge-x/api",
    "dev:web": "npm run dev --workspace @bridge-x/web",
    "build": "npm run build --workspace @bridge-x/api && npm run build --workspace @bridge-x/web",
    "build:vercel": "npm run build --workspace @bridge-x/web",
    "test": "npm run test --workspace @bridge-x/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
